.text-center.jumbotron
  %h1 Events
%br
.container
  %ul.list-group
    - @events.each do |event|
      %li.list-group-item.d-flex.justify-content-between.align-items-center
        .d-flex.flex-column
          = event.title
          .small= event.description
        .btn-group
          = link_to "View", event_path(event), class: "btn btn-outline-primary"
          = link_to "Edit", edit_event_path(event), class: "btn btn-outline-primary"
          = link_to "Delete", event_path(event), class: "btn btn-outline-danger", method: :delete, data: { confirm: "Are you sure?" }
    %br
    = paginate @events
  %br
  %a.btn.btn-primary{ href: new_event_path() } New event
  